# 更新日志

发版时 `release.yml` 会取出与 tag 同名的那一节，作为 GitHub Release 页面的正文。
所以**打 tag 前必须先在这里补上对应版本**，否则流水线会直接失败。

标题格式固定为 `## v<版本号> — <日期>`，节内容面向使用者写，构建产物说明由流水线自动附在末尾。
发版流程见 [PACKAGING.md](PACKAGING.md) 第 8 节。

## v1.2.0 — 2026-08-13

### 新增

- 设置页「关于」补上「检查更新」按钮，点击打开 GitHub Releases 页面。不做自动更新——目标机器很多在内网，自动检查大概率超时失败，也与「零网络请求、无遥测」的约定冲突。
- 「关于」面板显示的版本号改为构建时注入，不再手写。此前发到 v1.1.0 时那里仍写着 v0.1.0。

### 改进

- AI 总结的「取消」立即生效。原先点了取消，已经发出去的那一次请求停不掉，还得等它返回；模型卡住的话要等满 180 秒。现在取消信号与请求竞速，按下即返回。按钮文案相应从「取消（当前这段跑完后停下）」改回「取消」。

### 内部

- 版本号收敛到 `package.json` 一处，`tauri.conf.json` 指向它。此前散在三个文件里各写各的。
- 新增 CI：每次 push 与 PR 跑 typecheck、前端单测、Rust 测试与 clippy。此前只有发布流水线，日常提交没有任何自动检查。
- Release 页面的更新说明改为取自本文件。

## 未发布

### 新增

- **Excel 模板**（`.xlsx`）：用于月度工作盘点这类表。导入一张带表头的空表，DayLog 读出表头列名与表格下方的填写说明，把这段时间的工作按列的语义一行行填进去。列宽、合并单元格与说明文字原样保留，行数超过预留空行时说明区自动下移。表头要的信息记录里没有的（工时、完成率、项目编码这类）一律留空，不编造。「直接整理」模式离线可用。

### 改进

- AI 请求失败时展开完整错误链。此前只报一句「无法连接 AI 服务」，真正的原因（DNS 解析失败、证书不受信任、代理拒绝、连接超时）被丢弃，拿到报错也无从下手。

## v1.1.0 — 2026-08-03

### 变更

- **AI 总结不再走 `{{summary}}` 占位符**，改为按语义把区间记录归并成「工作项」，每项写成一段涵盖 SMART 五要素的连贯行文，并附「依据：MM-DD 共 N 条记录」溯源行。模板退化为只提供标题与章节名；含 `{{stats}}`、`{{entries_by_project}}` 等数据类占位符的章节仍由本地精确填充，数字不经模型的手。
- 长范围改为分段提取再合并。此前 30000 字符硬截断会让年度总结系统性丢失后半年的记录。
- 「直接整理」模式不受影响，行为与 v1.0.1 逐字节一致。

### 修复

- 月度 / 季度 / 年度三个内置模板缺 `{{summary}}`，导致选「AI 总结」时模型输出被整段丢弃——结果与「直接整理」完全相同。现各补一节可写章节。存量安装需在设置里点「恢复内置模板」才会更新（该操作会覆盖同名文件的本地修改）。

## v1.0.1 — 2026-07-31

### 修复

- 窗口一直开着跨天后，记录 / 回顾 / 报告三页的日期停在旧值，每天得手动改。现在跨零点自动更新；机器休眠唤醒后也会立即校正。手动改成过去日期的补记选择不受影响。

## v1.0.0 — 2026-06-29

### 新增

- 报告类型新增「月度总结」。

## v0.1.0 — 2026-06-17

首个版本。记录 / 回顾 / 报告三页，本地 SQLite 存储，Markdown 与 Word 模板，可选接入 OpenAI 兼容端点做 AI 总结与多日文本归类。
