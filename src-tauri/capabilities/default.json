{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：仅核心权限，无网络/文件系统暴露",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
